PORT=5000
# For Docker: use the service name "mongo" as the host
# For local dev (no Docker): use localhost
MONGODB_URI=mongodb://mongo:27017/college_review
JWT_SECRET=your_jwt_secret_here_change_this
JWT_EXPIRES_IN=7d
NODE_ENV=development
